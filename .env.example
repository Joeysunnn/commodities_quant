# Preferred for hosted deployments.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DB_NAME?sslmode=require

# Backward-compatible settings used by existing scripts.
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=commodities_db
DB_SSLMODE=require
